import { Plugin } from "aliucord/entities";
import { getByProps } from "aliucord/metro";
import { ApplicationCommandOptionType } from "aliucord/api";

export default class HideBlockedMessages extends Plugin {
    public async start() {
        //piss
    }
}